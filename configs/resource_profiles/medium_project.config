// =============================================================================
// Resource Profile: Medium Project (20-100 samples)
// Cluster: stokes | Account: tazarian | QOS: tazarian
//
// Standard production settings. Good balance between throughput and
// fair cluster sharing. Recommended default for most projects.
// =============================================================================

executor {
    queueSize       = 80
    submitRateLimit = '10/1min'
}
